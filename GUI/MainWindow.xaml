<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="Better Triggers" Height="450" Width="800" Background="#FF404040">
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" SnapsToDevicePixels="true" StrokeThickness="1" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="ButtonStyle1" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="125"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Menu Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Background="#FF282828" Foreground="White" >
            <MenuItem Header="File" Height="25"/>
            <MenuItem Header="Edit" Height="25"/>
            <MenuItem Header="New" Height="25"/>
            <MenuItem Header="JassHelper" Height="25"/>
        </Menu>

        <Button Style="{DynamicResource ButtonStyle1}" Grid.Row="1" Grid.Column="0" Width="20" Height="20" Margin="10,0,5,0" BorderBrush="#00000000" HorizontalAlignment="Left">
            <Button.Background>
                <ImageBrush ImageSource="Resources/Folder.png"/>
            </Button.Background>
        </Button>
        
        <Button Style="{DynamicResource ButtonStyle1}" Grid.Row="1" Grid.Column="0" Width="20" Height="20" Margin="35,0,5,0" BorderBrush="#00000000" HorizontalAlignment="Left">
            <Button.Background>
                <ImageBrush ImageSource="Resources/DocumentFunction.png"/>
            </Button.Background>
        </Button>

        <Button x:Name="btnSaveScript" Content="Save Script" Grid.Row="1" Grid.Column="1" Width="70" HorizontalAlignment="Left" Click="btnSaveScript_Click"/>

        <TreeView x:Name="treeViewTriggerExplorer" Grid.Row="2" Grid.RowSpan="2" Background="#FF282828" Margin="5,5,5,5" Foreground="White" AllowDrop="True">
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <EventSetter Event="TreeViewItem.DragOver"  Handler="treeViewTriggerExplorer_DragOver"/>
                    <EventSetter Event="TreeViewItem.Drop" Handler="treeViewTriggerExplorer_Drop"/>
                    <EventSetter Event="TreeViewItem.MouseMove" Handler="treeViewTriggerExplorer_MouseMove"/>
                    <EventSetter Event="TreeViewItem.MouseDown" Handler="treeViewTriggerExplorer_MouseDown"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
        <GridSplitter Grid.Row="1" Grid.RowSpan="4" Grid.Column="0" Width="5" Background="#FF404040"/>

        <avalonedit:TextEditor x:Name="textBoxTriggerComment" Grid.Row="2" Grid.Column="1" Background="#FF282828" Foreground="#9CDCFE" Margin="0,5,5,5" FontFamily="Consolas"/>
        <TreeView x:Name="treeViewTriggerFunctions" Grid.Row="3" Grid.Column="1" Background="#FF282828" Margin="0,5,5,5" Foreground="#FFFFFF"/>
        <GridSplitter Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3" Height="5" Background="#FF404040" ResizeDirection="Rows" HorizontalAlignment="Stretch" VerticalAlignment="Top" />

    </Grid>
</Window>
